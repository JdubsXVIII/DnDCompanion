import java.util.Random;
public class Roll {
    /**
     * Prints out a roll's total.
     * @param die The kind of die rolled.
     * @param diceNum The number of dice rolled.
     * @param mod The modifier used on the roll.
     */
    public Roll(int die, int diceNum, int mod){}
}
